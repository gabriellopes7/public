import random
from time import sleep

jogo =[]
listaJogos=[]
n = int(input('Digite a quantidade de jogos desejada: '))
for i in range(0,n):
    jogo[random(1,60),random(1,60),random(1,60),random(1,60),random(1,60),random(1,60)]
    listaJogos.append(jogo)
    print(f'Jogo {i}: {listaJogos[i]}')
    sleep(1)
print('Boa sorte !')
